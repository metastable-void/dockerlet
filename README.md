# dockerlet
Minimal Docker test-container helper. A small wrapper over bollard with a deliberately narrow feature set (unix-socket only, no rustls), intended as a lightweight alternative to the broader testcontainers crate for projects that don't need its full surface area.
